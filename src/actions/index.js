export const move = move => ({
    type: 'MOVE',
    move
})

export const draw = draw => ({
    type: 'DRAW',
    draw
})

export const deal = deal => ({
    type: 'DEAL',
    deal
})

export const pause = pause => ({
    type: 'PAUSE',
    pause
})